{
  "_comment": "The three states no file can prove, keyed <ledger>#<batch>: running, checked, skipped. Everything else the site shows is read off the files by npm run manifest. Only write \"checked\" if a person really has compared the batch with the photographs, sheet by sheet. Nothing is claimed here yet: Book I batch 1 is drafted and has a record edition, and both of those are observed rather than declared."
}
